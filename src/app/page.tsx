// Backend integration point: fetch passcode config, photo URL, and unlock page content from API
import PasscodeEntryScreen from './passcode-entry-screen/components/PasscodeEntryScreen';

export default function HomePage() {
  return <PasscodeEntryScreen />;
}